<div class="header">
    <div>
        <form action="/" method="get"><input type="submit" value="Home" class="link-button"/></form>
    </div>
    &nbsp;|&nbsp;
    <div>
        <form action="/users/new" method="get"><input type="submit" value="New user" class="link-button"/></form>
    </div>
</div>

<h2>Listing users</h2>

<ul>
<% @users.each do |user| %>
    <li> <%= user.email %>
    <%= link_to "Edit", "users/#{user.id}/edit" %>
    <%= link_to "Destroy", "users/#{user.id}/destroy", onclick: "return confirm('Are you sure?')" %>
<% end %>
</ul>
