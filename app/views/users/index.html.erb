<h1>Listing users</h1>

<ul>
<% @users.each do |user| %>
  <li> <%= user.email %> <%= link_to 'Edit', edit_user_path(user) %> <%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>
</ul>
